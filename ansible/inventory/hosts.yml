---
# Single target: a headless Debian host. Add real hosts under `all`, or leave it
# empty and target localhost:  ansible-playbook -i 'localhost,' -c local site.yml
all:
  hosts:
    # vps01:
    #   ansible_host: fda7:76ff:d1f2:3::15
    #   ansible_user: deploy
