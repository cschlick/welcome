---
galaxy_info:
  role_name: root_password
  author: cschlick
  description: Remove the root password entirely (empty the /etc/shadow field, not just lock it)
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies: []
