---
# Locking prefixes root's shadow password field with `!`. This preserves any
# existing hash for administrative recovery while making it unusable for
# password authentication. SSH separately enforces PermitRootLogin no.
- name: Lock root password authentication
  ansible.builtin.user:
    name: root
    password_lock: true
