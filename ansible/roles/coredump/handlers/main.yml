---
- name: Reload sysctl
  ansible.builtin.command: sysctl --system
  changed_when: false
  when: coredump_apply

- name: Reload systemd daemon
  ansible.builtin.systemd_service:
    daemon_reload: true
