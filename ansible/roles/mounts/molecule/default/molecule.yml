---
# Image is pre-built from roles/ssh/molecule/default/Dockerfile
# (tag: molecule-trixie-systemd):
#   docker build -t molecule-trixie-systemd ../../../ssh/molecule/default
# Needs: pip install molecule "molecule-plugins[docker]" ansible; plus docker.
# Only /run is pre-mounted as tmpfs here (systemd needs it); /tmp is left a
# plain dir so the role cleanly mounts its hardened tmpfs over it.
role_name_check: 1
driver:
  name: docker
platforms:
  - name: mounts-trixie
    image: molecule-trixie-systemd
    pre_build_image: true
    command: /lib/systemd/systemd
    privileged: true
    cgroupns_mode: host
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    tmpfs:
      - /run
provisioner:
  name: ansible
  env:
    # Make the role under test resolvable (it lives at ../.. = roles/mounts).
    ANSIBLE_ROLES_PATH: "${MOLECULE_PROJECT_DIRECTORY}/.."
verifier:
  name: ansible
